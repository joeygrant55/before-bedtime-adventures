# Before Bedtime Adventures - Environment Variables
# Copy this file to .env.local and fill in your values

# ============================================
# CONVEX (Required)
# ============================================
# Run 'npx convex dev' to get these values
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=

# ============================================
# CLERK AUTH (Required)
# ============================================
# Get from https://clerk.com/dashboard
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# ============================================
# GEMINI AI (Required for image transformation)
# ============================================
# Get from https://ai.google.dev/
GEMINI_API_KEY=

# ============================================
# STRIPE (Required for payments)
# ============================================
# Get from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...

# Webhook signing secret - get from Stripe dashboard after creating webhook
# For local development, use Stripe CLI: stripe listen --forward-to localhost:3000/api/stripe/webhook
STRIPE_WEBHOOK_SECRET=whsec_...

# ============================================
# LULU PRINT API (Required for printing)
# ============================================
# Get from https://developers.lulu.com/
# Create an application to get client key and secret

LULU_CLIENT_KEY=
LULU_CLIENT_SECRET=

# Set to "true" for sandbox testing, "false" for production
LULU_USE_SANDBOX=true

# ============================================
# APP CONFIGURATION
# ============================================
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# NOTES
# ============================================
# 
# Stripe Webhook Setup:
# 1. Go to https://dashboard.stripe.com/webhooks
# 2. Click "Add endpoint"
# 3. Enter your webhook URL: https://your-domain.com/api/stripe/webhook
# 4. Select events: checkout.session.completed, checkout.session.expired
# 5. Copy the webhook signing secret to STRIPE_WEBHOOK_SECRET
#
# For local testing with Stripe CLI:
#   stripe listen --forward-to localhost:3000/api/stripe/webhook
#
# Lulu API Setup:
# 1. Create an account at https://developers.lulu.com/
# 2. Create a new application
# 3. Copy the client key and secret
# 4. For testing, use LULU_USE_SANDBOX=true
#
# Convex Environment Variables:
# Some of these env vars need to be set in Convex too:
#   npx convex env set LULU_CLIENT_KEY "your-key"
#   npx convex env set LULU_CLIENT_SECRET "your-secret"
#   npx convex env set LULU_USE_SANDBOX "true"
